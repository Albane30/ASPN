{% extends 'front/layout.html.twig' %}

{% block title %}ASPN{% endblock %}

{%block stylesheets %}
{{ parent() }}
{% endblock %}

{% block content %}
<div class="content-result">
    <h1 class="text-center pt-3">Résultats</h1>
 {% for result in results %}
            <div class="mx-md-5 py-2">
                <p>{{ result.date ? result.date|date('d-m-Y H:i:s') : '' }}</p>
                <p>{{ result.content }}</p>
            </div>
{% endfor %}
</div>
{% endblock %}