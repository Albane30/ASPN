{% extends 'front/layout.html.twig' %}

{% block title %}ASPN{% endblock %}

{%block stylesheets %}
{{ parent() }}
{% endblock %}

{% block content %}
<section id="content-program">   
    <div class="row pb-5">
         <div class="col-md-6">
            <h1 class="program-title text-center pt-3">Programme</h1>
            {% for program in programs %}
            <div class="card card-program mx-md-5 py-1 my-3">
                <div class="card-body">
                    <p>Le {{ program.date ? program.date|date('d-m-Y à H:i') : '' }}</p>
                    <p>{{ program.content }}</p>
                </div>
            </div>
{% endfor %}
         </div>          
    </div>
</section>
{% endblock %}