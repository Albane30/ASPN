{% form_theme form 'admin/team/_form_theme.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn m-4">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
