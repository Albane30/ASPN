{% extends 'base.html.twig' %}

{% block title %}Modifier licencié{% endblock %}

{% block body %}
    <h1 class="text-center my-3">Modifier un licencié</h1>

    {{ include('admin/user/_form.html.twig', {'button_label': 'Modifier'}) }}
    <div class="text-center">
        <a class="btn my-3" href="{{ path('admin_U_user_index') }}">Retour</a>
    </div>
    {{ include('admin/user/_delete_form.html.twig') }}
{% endblock %}
